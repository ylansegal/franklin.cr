Searched for: <%= @search_terms %>
<% @collated_results.each do |item, availabilities| -%>
======================================================
<%= item.title %>
By <%= item.author %>
Format: <%= item.format %>
Availability:
<% availabilities.map {|a| AvailabilityDescription.new(a, item) }.each do |availability| -%>
  <%= availability.to_s %>
    <%= availability.url %>
<% end -%>
<% end -%>
